name: Run Iftar Bot

on:
  schedule:
    # 5:00 AM IST (Punjab) = 23:30 UTC
    - cron: "30 23 * * *"
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 70

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: |
          npm install
          npx playwright install --with-deps

      - name: Run Iftar Test Only
        run: npx playwright test tests/autoRegister.spec.ts --project=chromium --workers=1 --reporter=line